@startuml
actor User
participant ":Parser" as Parser
participant ":TransactionsList" as List
participant ":Transaction" as Trans
participant ":Storage" as Store

User -> Parser : "edit 1 -desc NewDesc -p ..."
Parser -> List : editTransaction(id, date, desc, postings, currency)
List -> List : findById(1)
List -> Trans : update(date, desc, postings, currency)
Trans -> Trans : isBalanced()
note right: Validation fails if\nAssets != Equity...
Trans --> List : Success
List -> Store : save(transactions)
List --> User : "Transaction edited successfully"
@enduml
